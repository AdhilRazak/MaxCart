<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - MaxCart</title>
    <style>
        body {
            font-family: Arial, sans-serif;

            background-color: #0e4580;
        }
        .container {
            width: 1200px;
            height: 600px;
            margin: 50px auto;
            border-radius: 5px;
            background-color: #0e4580;
            color: #000;
        }
        .input-group {
            margin-bottom: 20px;
        }
        .input-group label {
            display: block;
        }
        .input-group input {
            width: 80%;
            margin-top: 20%;
            margin-left: 10%;
            padding: 10px;
            font-size: 16px;
            border-radius: 5px;
            border: 1px solid #ccc;
        }
        h1{
        margin-left: 4%;
        padding-top: 5%;
        }
        .btn {
            display: block;
            width: 30%;
            margin-top: 1%;
            margin-left: 62%;
            padding: 10px;
            font-size: 16px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Forgot Password</h1>
        <form action="/forgotemail" method="post">
            <div class="input-group">
                <input type="email" id="email" name="email" placeholder="Please enter your email address to receive an OTP for password reset">
            </div>
            <button type="submit" class="btn" >Send OTP</button>
        </form>
    </div>
</body>
</html>
